export const ROUTES = {
  LOGIN: "/login",
  REGISTER: "/register",
  PRODUCTS: "/products",
  CART: "/cart",
  ORDERS: "/orders",
  PROFILE: "/profile",
} as const;

export const DEFAULT_PAGE = 1;
export const DEFAULT_LIMIT = 12;
